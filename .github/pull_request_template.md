## Change

Describe the problem and resulting behavior.

## Validation

Record the relevant local checks and any blocked verification.

OpenSpec changes: none

Replace `none` with comma-separated change IDs when this implementation belongs
to an OpenSpec change, even if no spec files appear in the diff. Reviewers verify
this association and the affected requirements before approval.
